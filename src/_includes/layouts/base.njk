<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>
<body>
    {% include "parts/header.njk" %}
    <main>
        <h1>{{ title }}</h1>
        {{ content | safe }}
    </main>
    <nav>
        {{ content | toTOC }}
    </nav>
</body>
</html>